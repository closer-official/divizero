# Claude Code向け：営業期待値チェック方式 実運用・不具合監査指示書

## 目的

営業期待値スコアを「AIによる数値採点」から「人間が確認項目をチェックし、アプリが自動計算する方式」へ変更した。

今回の監査では、コードがビルドできるかだけでなく、実際の営業運用で迷わず使えるか、既存案件を壊さないか、画面間で値が食い違わないかを調査すること。

**この依頼では修正を行わないこと。**  
コード・プロンプト・データを変更せず、問題点、再現手順、原因、推奨修正方針をレポートすること。

---

## プロジェクト概要

- React 18 + TypeScript + Vite
- Firestoreの `workspace/main.payload` に `AppData` 全体をJSON文字列で保存
- Firestore未接続時は `localStorage('os_data_v1')`
- アプリ自身はAI APIを呼ばない
- OS①プロンプトを外部AIで実行し、結果をアプリへ貼り付ける
- 営業期待値は40点表記だが、新方式の理論上の最高点は39点

作業開始前に以下を読むこと。

1. `AGENTS.md`
2. `src/types.ts`
3. `src/utils/salesExpUtils.ts`
4. `src/components/tabs/Tab1.tsx`
5. `src/components/tabs/Tab2.tsx`
6. `src/utils/parser.ts`
7. `src/hooks/useData.ts`
8. 今回の未コミット差分（`git diff`）

---

## 今回の仕様

### 新しい確認項目と配点

| 項目 | 点数 |
|---|---:|
| 教育者であることを確認 | +8 |
| UTAGE利用を確認 | +8 |
| 受講生・コミュニティ会員を確認 | +8 |
| LINE販売または高単価無形商材を確認 | +6 |
| note・LP等の販売導線を確認 | +5 |
| 無形商材販売を確認 | +4 |
| LP・HPが既に存在 | -3 |

計算結果は `0〜40` に丸める。全加点項目ON・減点OFFなら39点、全加点ON・減点ONなら36点。

### 後方互換

- 新方式：`salesExpectationFacts`
- 旧方式：`salesExpectation`
- `salesExpectationFacts` が存在すればチェック算出値を使用
- 存在しなければ旧 `salesExpectation` を表示・判定に使用
- `salesExpectationReason` と `salesExpectationBreakdown` は削除しない

### 主な変更対象

- `src/types.ts`
- `src/utils/salesExpUtils.ts`
- `src/components/tabs/Tab0.tsx`
- `src/components/tabs/Tab1.tsx`
- `src/components/tabs/Tab2.tsx`
- `src/utils/helpers.ts`
- `src/utils/os2Prompt.ts`
- `src/utils/s1ActionPrompt.ts`
- `src/utils/batchS1ActionPrompt.ts`
- `src/utils/parser.ts`
- `src/App.tsx`
- `public/prompts/OS1_X_接触スクリーニング_latest.md`
- `public/prompts/OS1_Instagram_接触スクリーニング_latest.md`
- `public/prompts/OS1_Threads_接触スクリーニング_latest.md`

---

## 監査の進め方

### 1. 静的調査

次を検索し、営業期待値の参照・保存・コピー箇所を漏れなく一覧化すること。

```bash
rg -n "salesExpectation|salesExpectationFacts|salesExpectationReason|salesExpectationBreakdown|getDisplayScore|calcSalesExpectationScore|SALES_EXP_ITEMS" src public/prompts
```

以下を確認すること。

- 表示、フィルタ、保護判定、プロンプト生成がすべて同じ表示スコアを使っているか
- `salesExpectation` を直接参照して新方式と食い違う箇所が残っていないか
- TargetからPipelineItemへ移る全経路でFactsがコピーされるか
- Target側でチェック変更後、すでに作成済みのPipelineItemとも同期するか
- PipelineItem側で変更したFactsをTargetへ戻す必要があるか。必要なら現状の片方向同期が実運用上の問題になるか
- OS③への引き継ぎで型を追加しただけになっておらず、実際に利用・保存されるか
- viewerロールで閲覧でき、変更できないか
- optionalフィールド追加により既存JSONの読み込みが壊れないか
- 空オブジェクト `{}` と `undefined` の意味が正しく区別されているか
- 全チェックをOFFにした後も新方式の0点として扱われるか
- チェック操作のたびにFirestoreへ全payload保存が走る設計が、連打・通信遅延・競合時に問題を起こさないか

### 2. 特に疑わしい実装を重点確認

以下は必ずコードを確認し、問題かどうかを判定すること。

1. `Tab2.tsx` に旧AI採点UI・旧編集state・miniPromptが、`false && ...` で非表示にしただけで残っていないか
2. 非表示コードが保守性、バンドルサイズ、意図しない再表示、lint上の問題を起こさないか
3. Tab1の詳細画面でFacts未設定の既存案件が、ヘッダ上は `0 / 40点`、別行では旧AIスコアと表示され、利用者を混乱させないか
4. OS①の通常フローが登録直後にPipelineを自動生成してTab2へ遷移するため、Tab1の「初回チェックUI」を実際には操作しにくくないか
5. OS①プロンプト内に「AIは採点しない」と、FT/NT判定の「営業期待値スコア35点以上」など矛盾する指示が残っていないか
6. 新しい【営業期待値 確認候補】ブロックをparserが正しい範囲で抽出できるか
7. OS①バッチ出力で複数アカウントが連結された際、確認候補が別アカウントの内容まで取り込まれないか
8. `salesExpectationBreakdown` に見出しを除く候補本文が期待どおり保存されるか
9. 新方式の最大39点に対して、超優良判定35点が実際に到達可能で、意図した組み合わせになるか
10. 減点項目だけONの場合、0点に丸められる挙動が利用者に理解できる表示か
11. Tab2でFactsを編集した後、Kanban、CaseCard、超優良フィルタ、保護バッジが同じレンダー内で更新されるか
12. 35点をまたいで変更したとき、sleeping/archived案件の保護表示や実際の状態遷移ロジックに矛盾がないか

### 3. ビルド・型・品質確認

最低限、次を実行すること。

```bash
npm run build
git diff --check
```

利用可能なlint/testスクリプトが `package.json` にあれば、それも実行すること。  
テストが存在しない場合は「自動テストなし」と明記し、追加すべき単体テストを具体化すること。

`dist/` はビルド生成物である。監査中に生成差分が出ても、ソースの問題と混同しないこと。また、監査依頼ではコミット・push・deployをしないこと。

### 4. 計算ロジックの検証

最低限、以下を検証すること。

| ケース | 期待値 |
|---|---:|
| Factsなし、旧スコア35 | 35 |
| Facts `{}`、旧スコア35 | 0 |
| +8を1つ | 8 |
| +8を3つ、+6、+5 | 35 |
| 全加点ON、減点OFF | 39 |
| 全項目ON | 36 |
| 減点のみON | 0 |

`getDisplayScore()` のFacts優先が、TargetとPipelineItemの両方で成立することを確認すること。

### 5. 実画面による運用テスト

可能ならローカル開発サーバーを起動し、デスクトップ幅とモバイル幅の両方で確認すること。

実データを壊さないよう、ローカルのテストデータまたは一時的なlocalStorageのみを使うこと。外部Firestore本番データは変更しないこと。

#### 新規案件

1. X、Instagram、Threadsそれぞれの新形式AI出力を貼り付ける
2. 確認候補が参考表示される
3. AI数値スコアが保存されない
4. チェックをON/OFFするとスコアが即時変化する
5. リロード後もチェック状態が残る
6. Tab1からTab2への移行後も同じチェックと点数になる

#### 既存案件

1. `salesExpectation: 35`、Factsなし
2. Kanbanで期待35と表示
3. 超優良フィルタに含まれる
4. CaseCardで旧AIスコアと明示される
5. 初めてチェックした時点から新方式へ切り替わる
6. 旧値と新値が同時に主スコアとして表示されない

#### 境界値

1. 34点相当から35点相当へ変更
2. 超優良フィルタ・バッジが即時更新
3. 35点相当から減点項目ONで32点相当へ変更
4. フィルタ対象・保護表示が即時解除

#### 権限・操作性

- adminではチェック可能
- viewerではチェック不可
- checkboxだけでなくラベルを押しても操作できる
- モバイルで横切れ、文字の重なり、押しにくさがない
- 7項目と点数の対応が視線移動少なく理解できる
- 保存ボタンがない「即時保存」であることが利用者に理解できるか
- 保存中・保存失敗が分からないことが実運用上の事故につながらないか
- 誤クリックを戻しやすいか

### 6. プロンプト実運用監査

3つのOS①プロンプトを比較し、次を確認すること。

- X / Instagram / Threadsで確認候補の項目、順序、表記が一致
- AIへ数値採点を要求する文言が残っていない
- 出力フォーマットの別セクションにも旧スコア欄が残っていない
- FT/NT/UT判定ルールと「AIは採点しない」が矛盾しない
- AIが「あり / なし / 不明」のいずれかを安定して出力できる指示になっている
- 「確認候補あり」と「あり」が混在していない
- 根拠メモがどの項目に対応するか人間が判別できる
- AI出力を見ながら人間がチェックする際、誤認しやすい項目の重複がない
- 「LINE販売または高単価無形商材」と「無形商材販売（上記以外）」を同時加点してよい仕様か。二重加点の意図を確認すべきか
- 「note・LP等の販売導線」と「LP・HPが既に存在」の加点・減点が同時成立する条件が明確か

---

## 実運用の評価観点

単なるバグだけでなく、次の観点で評価すること。

### 入力負荷

- 1案件につき7チェックは現実的か
- AI候補を見ながらチェックしやすい配置か
- 候補が折りたたまれていることで余計な操作が増えていないか
- Tab1とTab2のどちらでチェックするのが実際の主動線か明確か

### 判断の一貫性

- 「確認済み」の定義が利用者によってぶれないか
- AI候補をそのまま信用して誤チェックする危険がないか
- 根拠URLやメモを項目別に残せないことが後日の再確認を難しくしないか

### データの信頼性

- 誰がいつチェックしたか記録がないことは問題か
- 後からチェック変更できるのに「OS①確定値」と表現する箇所が残っていないか
- TargetとPipelineItemに複製されたFactsが将来不一致にならないか
- Firestoreのリアルタイム同期で複数端末編集時にlast-write-winsの事故が起きないか

### フィードバック

- 即時保存の成功・失敗が利用者に伝わるか
- 旧方式から新方式へ切り替わった瞬間が理解できるか
- 旧AIスコアを新チェックへ戻す、または未設定へ戻す操作が必要か

---

## レポート形式

最初に結論を記載し、その後に以下の順で報告すること。

### 1. 総合判定

以下のいずれか。

- 本番運用可能
- 軽微な修正後に本番運用可能
- 重大な修正が必要
- 現状では本番運用不可

判定理由を3〜5行で記載。

### 2. 発見事項

深刻度順に並べること。

| ID | 深刻度 | 分類 | 問題 | 実運用への影響 | 再現手順 | 原因箇所 | 推奨修正 |
|---|---|---|---|---|---|---|---|

深刻度は以下。

- Critical：データ破損、既存案件消失、本番全体が使えない
- High：誤スコア、誤フィルタ、主要動線が成立しない
- Medium：混乱、二重管理、保存状態が分かりにくい
- Low：表示、保守性、軽微な操作性

問題がない項目も「確認済み・問題なし」として根拠を記載すること。

### 3. 動線別テスト結果

- OS①新規単体登録
- OS①バッチ登録
- Tab1詳細編集
- Tab1 → Tab2
- Tab0 → Tab2
- Tab2編集
- 既存案件の後方互換
- 超優良フィルタ・保護表示
- OS②/S1/OS③引き継ぎ
- viewer権限
- モバイル表示

各項目を `Pass / Fail / 未確認` で記載。

### 4. 計算テスト結果

上記7ケースの実測結果を表にすること。

### 5. プロンプト監査結果

3ファイルごとの差異、矛盾、AI出力の安定性リスクを記載。

### 6. 推奨対応順

修正する場合の順序を、以下に分けること。

1. 本番投入前に必須
2. 早期対応推奨
3. 将来改善

### 7. 未確認事項

環境や認証の都合で確認できなかった項目を明記すること。推測でPassにしないこと。

---

## 禁止事項

- 調査中にコードを修正しない
- `npm run deploy` を実行しない
- commit、push、PR作成をしない
- 本番Firestoreデータを変更しない
- 既存の未コミット変更を破棄・整形しない
- 問題を見つけても、証拠なしに断定しない

最終レポートでは、必ずファイルパスと行番号を示し、可能なものは具体的な再現手順を添えること。
