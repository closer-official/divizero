# Claude Code 調査指示書：モバイルUI修正のバグ・回帰確認

## 目的

直近で実装したモバイルUI修正について、以下を調査してください。

- 新たなバグや表示崩れがないか
- 既存機能が動かなくなっていないか
- PC・タブレット表示に回帰がないか
- モバイルで操作不能、誤操作、横あふれが発生しないか

今回は**調査と報告のみ**行ってください。問題を発見しても、コード修正・コミット・push・デプロイはしないでください。

## 対象リポジトリ

`C:\Users\tduka\divizero`

## 今回の変更対象

- `src/index.css`
- `src/App.tsx`
- `src/components/tabs/Tab1.tsx`
- `src/components/tabs/Tab2.tsx`

未コミット差分を確認し、直前のコミットとの差分を今回の変更として調査してください。

```bash
git status --short
git diff -- src/index.css src/App.tsx src/components/tabs/Tab1.tsx src/components/tabs/Tab2.tsx
```

## 実装した内容

### 1. ナビゲーション

- 640px未満ではタブ文字を短縮
- モバイルのタブ文字を11px、paddingを10pxへ縮小
- 640px以上では従来のラベル・サイズを維持

### 2. 共通ボタン

- `.btn-primary` と `.btn-sec` に `min-height: 44px` を追加

### 3. Tab1

- TargetDetailをモバイルではボトムシート表示
- 背景タップで閉じる
- シート内タップでは閉じない
- 640px以上では従来どおりインライン表示

### 4. Tab2

- 640px未満ではカンバンを非表示にし、案件を縦リスト表示
- リストは既存のフィルターと優先度ソート結果を利用
- 案件タップで既存ドロワーを開く
- モバイルではドロワーを全画面表示
- 640px以上では従来のカンバンを表示
- リサイズハンドルはモバイルで非表示

## 変更禁止範囲

今回の調査では、以下を変更しないでください。

- Gemini遷移・clipboardロジック
- Firestore・localStorage・Auth
- 連続処理モード
- 面談待ち判定
- プロンプトファイル
- URL生成・外部リンク
- データ型・保存形式
- `dist/`を含む生成物

## 必須調査

### A. 静的確認

1. TypeScriptエラー、未使用変数、構文不備
2. Tailwindのレスポンシブクラスが正しく生成されるか
3. 同一コンポーネントの二重表示が起きないか
4. `selectedId`、`drawerItemId`、`continuousMode`の状態遷移が壊れていないか
5. 背景クリックのイベント伝播が意図どおりか
6. モバイル用リストがカンバンと同じフィルター・ソート条件か
7. `drawerWidth`のinline styleがモバイルの`w-full`を上書きするケースがないか
8. `.btn-sec`の44px化により、ヘッダー・表・アイコンボタンなど既存UIが崩れていないか

### B. ビルド確認

```bash
npm run build
```

以下を報告してください。

- 成否
- エラー内容
- 今回の変更に起因する警告
- 既存警告と判断したもの

### C. 表示確認

可能ならブラウザで以下の幅を確認してください。

- 390×844：スマートフォン
- 768×1024：タブレット
- 1280×720：PC

各サイズで、少なくとも以下を確認してください。

#### 共通

- ページ全体に意図しない横スクロールがない
- タブが操作可能
- アクティブタブが判別可能
- ヘッダーがコンテンツを隠さない
- Toast、モーダル、ドロワーの重なり順

#### Tab1

- 対象行タップで詳細が開く
- 390pxではボトムシートになる
- 640px以上ではインライン詳細になる
- 背景タップと×ボタンで閉じる
- シート内部の操作で誤って閉じない
- プロフィール、OS⓪に戻す、OS②へ移行、コピー、削除が操作可能
- ボトムシート内を最下部までスクロールできる
- 連続処理モードが従来どおり動く

#### Tab2

- 390pxでは縦リストだけが表示される
- 640px以上ではカンバンだけが表示される
- フィルター、state、チャネル、検索がモバイルリストへ反映される
- 優先度順が既存カンバンと一致する
- 案件タップで正しい案件のドロワーが開く
- 390pxのドロワーが画面幅100%になる
- ドロワー内がスクロール可能
- 閉じるボタンが操作可能
- PCではリサイズハンドルが動く
- モバイルではリサイズハンドルが表示されない
- 連続処理の前へ・次へ・終了が動く
- カンバンのリアクション記録がPCで従来どおり動く

### D. 既存機能の回帰確認

最低限、以下のコードパスに今回の変更による影響がないか確認してください。

- Tab0〜Tab6およびHOMEのタブ遷移
- viewer/adminによる表示制御
- コピー後のGemini遷移
- Tab1からTab2への案件移行
- Tab2のタッチ追加・反応記録・DM会話
- Tab2からTab3へのクローズ
- 削除・確認ダイアログ
- Firestore未接続時のlocalStorageフォールバック

外部サービスへ実データを送信したり、実データを削除したりしないでください。必要ならテスト用データまたはコードリーディングで確認してください。

## 特に疑うべきポイント

以下は重点的に確認してください。

1. `drawerWidth`が一度PCで設定された後に画面幅をスマホへ変えると、全画面にならない可能性
2. TargetDetailをモバイル・PC用に2箇所記述したことによるコールバック差異
3. ボトムシートの内側に既存の`.card`があり、余分な枠・角丸・余白が出る可能性
4. `.btn-sec { min-height:44px }`が小型ボタンにも一律適用され、レイアウトを押し広げる可能性
5. 空データ時と案件あり時で、Tab2のレスポンシブ切替結果が異なる可能性
6. タブ短縮後もバッジ数が大きい場合にナビ幅が過大になる可能性
7. ボトムシート表示中に背面ページがスクロールする可能性
8. モバイルリストでリアクションの状態や警告が見えず、業務判断に必要な情報が欠落していないか

## 報告形式

問題がある場合は、重要度順に以下の形式で報告してください。

```text
[P0/P1/P2/P3] 問題の短いタイトル

対象:
- ファイル:行番号
- 画面サイズ・条件

現象:
- 何が起きるか

再現手順:
1.
2.
3.

期待結果:
- 本来どうなるべきか

原因:
- コード上の根拠

推奨修正:
- 最小限の修正案

影響範囲:
- 影響する画面・操作
```

重要度の基準：

- P0：データ破壊、保存不能、アプリ起動不能
- P1：主要業務が実行不能、誤った案件を操作する危険
- P2：一部操作不能、重大な表示崩れ、明確な回帰
- P3：軽微な表示・操作性の問題

最後に以下をまとめてください。

```text
総合判定: リリース可 / 条件付きリリース可 / リリース不可

ビルド結果:
確認した画面サイズ:
発見件数: P0=?, P1=?, P2=?, P3=?
未確認項目:
既存機能への回帰:
```

問題が見つからなかった場合も、「問題なし」だけで終わらせず、確認した項目、確認できなかった項目、残存リスクを明記してください。
