# 営業OSワークスペース 監査報告書（3本独立実施）

実施日：2026-07-03
実施者：Claude Fable 5（本環境にサブエージェント機構がないため直接実施。委譲設計は修正タスク分解に記載）
根拠主義：全指摘にファイル名・行番号を付す。実ファイルで確認できないものは「要確認」。

---

# 監査1：指示ファイル監査

## 監査対象一覧

| ファイル | 行数 | 状態 |
|---|---|---|
| CLAUDE.md | 239行 | 実読済み |
| 00_handoff_overview.md | 29行 | 実読済み |
| 01_handoff_flow_and_scope.md | 51行 | 実読済み |
| AGENTS.md | — | **未提供。存在自体が要確認** |
| .claude/ 配下 | — | **未提供。存在自体が要確認** |
| public/prompts/（AI指示の実運用契約） | 4/18本 実読 | OS0全般版・OS1_X・OS2・OS3のみ提供 |

## 総評

CLAUDE.md は構成自体は良質（読解順の指定、テーブル整理、S∞構造の説明）だが、**239行で200行基準を超過**しており、かつ**実装（types.ts / App.tsx）との乖離が3箇所**確認された。ドキュメントの鮮度が落ちると Claude Code の判断がここから狂うため、乖離の修正が行数削減より先。プロンプトファイル側は「Human-Check Loop」の**完全同文の重複が4ファイルで確認**され（提供分だけで約280行分）、うち2ファイル（OS0・OS3）では機能しない死んだ記述になっている。

## 重要度順の問題一覧

### P1. CLAUDE.md と実装の矛盾（3件）

**1-a. PipelineItem.state に `meeting_scheduled` が実装済みだが CLAUDE.md に記載なし**
- 根拠：types.ts L283 `state?: 'active' | 'waiting' | 'meeting_scheduled' | 'sleeping' | 'archived' | 'closed'`
- 根拠：App.tsx L117, L131 で `meeting_scheduled → active` の起動時遷移が実装済み
- 一方 CLAUDE.md「S∞ループ構造」節は `'active' | 'waiting' | 'sleeping' | 'archived' | 'closed'` の5値のみ記載
- 影響：Claude Code が state を触る修正をした場合、meeting_scheduled を壊すリスク

**1-b. Track に `UT` が実装済みだが CLAUDE.md は「FT / NT / SKIP」のみ**
- 根拠：types.ts L2 `export type Track = 'FT' | 'NT' | 'UT' | 'SKIP'`
- 根拠：OS1_X_接触スクリーニング_latest.md L73「優先（FT）/ 通常（NT）/ UTAGEユーザー（UT）」、parser.ts L153 で UT 判定を実装
- CLAUDE.md 末尾「**Step:** S1→S5 / **Track:** FT（…）/ NT（…）/ SKIP（除外）」に UT が欠落
- 影響：UTトラックは運用上の最優先セグメントなのに、指示ファイル上は存在しないことになっている

**1-c. 「The six tabs」だが実装は8タブ**
- 根拠：App.tsx L14-15 で `Tab6` と `TabHome` を import、L349-357 のナビ配列は home / tab0〜tab6 の8件
- CLAUDE.md は Tab0〜Tab5 の6タブ構成として記述。TabHome（ホーム）と Tab6（人格OS）が構造図・タブ説明の両方から欠落
- 補足：Tab6 はハンドオフ上スコープ外だが、「存在するがスコープ外」と「存在しない」は別。TabHome は営業OS本体の導線（App.tsx L386-392 で pipeline アイテムへの直接遷移を持つ）なので記載漏れは実害あり

### P2. プロンプトファイルの重複・死んだ記述

**2-a. 「AI人間性監査（Human-Check Loop）」の完全同文コピーが4ファイルに存在**
- 根拠：OS0_一次選別_v2.md（約L120-180）、OS1_X L195-263、OS2 L120-170付近、OS3 L45-115付近。全て同一文面（5人審査員テーブル含む）
- 提供4本だけで約280行の重複。未提供14本にも同ブロックがある可能性が高い（要確認）
- 特に問題：**OS0 と OS3 ではこのブロックは死んでいる**
  - OS0 の役割は分類のみで「接触文・返信案・DM文」を一切生成しない（OS0 冒頭「このOSの役割は深い診断ではありません」、出力フォーマットに文面欄なし）
  - OS3 も検証レポートのみで提案文A/Bを生成しない（出力フォーマットは【案件ログ】〜【個別結論】）
  - つまり「接触文を生成したら実行する」ループの発動条件が構造的に満たされない。毎回のOS0実行で約70行（ファイルの約30%）が無駄トークンとして送信され、本来の判定指示を薄めている

**2-b. CLAUDE.md のプロンプト一覧と Prompts 型の不一致**
- 根拠：types.ts L616 に `S1_ACTION_BATCH?: string` が定義されているが、CLAUDE.md のプロンプトファイル対応表に該当ファイルの記載がない
- 対応ファイルが存在するのか、型だけ先行しているのかは未提供のため**要確認**

### P3. 200行超過と埋没

- CLAUDE.md は239行。超過分の主因は (1)「The six tabs」の各タブ詳細説明（実装を読めばわかる操作説明が多い）、(2) プロンプト18本の対応表、(3) S∞の目安日数などの運用パラメータ
- 「毎回読む必要のない情報」の混入例：Tab の UI 操作説明（「ページネーション: 10件ずつ表示」等）、deploy.js の内部手順詳細。これらは docs/ 送りで足りる

### P4. ハンドオフ文書間の軽微な重複

- 00_handoff_overview.md と 01_handoff_flow_and_scope.md は読解順・スコープ除外（Tab6）・「プロンプト＝契約」の3点が重複。統合しても情報損失なし（合計80行→50行程度に圧縮可能）

## 重複・矛盾マップ

| 記述 | 出現箇所 | 種別 |
|---|---|---|
| Human-Check Loop 全文 | OS0 / OS1_X / OS2 / OS3（＋未提供分要確認） | 重複（OS0・OS3では死文） |
| state 5値 vs 6値 | CLAUDE.md ↔ types.ts L283 | 矛盾 |
| Track 3値 vs 4値 | CLAUDE.md ↔ types.ts L2, parser.ts L153 | 矛盾 |
| 6タブ vs 8タブ | CLAUDE.md ↔ App.tsx L349-357 | 矛盾 |
| 営業期待値スコア（0-40） | CLAUDE.md記載なし / types.ts L294 / OS2は「旧互換参照値」として要求 | 三者不整合（詳細は監査3-P1） |
| Tab6スコープ除外 | 00_overview ↔ 01_flow | 重複（無害） |

## 200行以内に収めるための修正方針（未適用・提案のみ）

1. **矛盾3件の修正を最優先**（+3行程度で済む：meeting_scheduled、UT、8タブ構成の追記）
2. 各タブの UI 操作詳細（約40行）を `docs/tabs.md` へ分離し、CLAUDE.md にはタブ名・責務・入出力の1行ずつだけ残す
3. deploy の内部手順（約10行）を `docs/deploy.md` へ。CLAUDE.md にはコマンド3行と「prompts編集→deployで反映」の1行のみ
4. プロンプト対応表は残す（これは契約情報で毎回必要）。ただし S1_ACTION_BATCH の実態確認後に整合させる
5. ハンドオフ2文書は1本に統合
6. Human-Check Loop はプロンプト側の課題（監査3で扱う）だが、指示ファイル観点では「共通ブロックは1ファイル化し、必要なOSのみに含める」方針を記載しておく

見積り：上記で CLAUDE.md は約185行に収まる。

**修正は適用していません。承認後に実行します。**

---

# 監査2：プロジェクト全体監査

## エグゼクティブサマリー

提供された範囲（ルート・型・パーサ・プロンプト4本）を実読した結果、**アプリの骨格は一貫しており、致命的なバグは確認範囲では見つからなかった**。一方で、(1) 全データを Firestore 単一ドキュメントの JSON 文字列に保存する設計がデータ喪失の構造リスクを抱えている、(2) 認証がクライアント側ゲートに見え Firestore ルールの実態が未確認、(3) パーサとプロンプトの契約に取りこぼしが2箇所ある、(4) データモデルに新旧二重構造が残存している、の4点が主要な指摘。テスト構成・エラー処理・api/ は**ファイル未提供のため要確認**とする。

## 監査対象の棚卸し

- 実読：App.tsx（463行・全行）、types.ts（631行・全行）、parser.ts（377行・全行）、プロンプト4本、CLAUDE.md、ハンドオフ2本
- 未提供（要確認）：useData.ts / useAuth.ts / usePrompts.ts / helpers.ts / api/auth.js / api/config.js / deploy.js / 各Tab / package.json / Firestoreセキュリティルール / テストファイルの有無

## 監査結果（重要度順）

### A1.【データ喪失リスク・構造】単一ドキュメント JSON payload 方式

- 根拠：CLAUDE.md「全データは Firestore の `workspace/main` ドキュメントの `payload` フィールドに JSON 文字列として保存」
- 根拠：types.ts で raw テキストを大量に抱える設計 — Target.rawInput / aiOutput（L188-189）、Touch.os2RawOutput / dmRawOutput 等（L24, L29, L69）、ClosedDeal.rawOutput（L331）、Analysis.rawOutput（L404）
- 問題：Firestore のドキュメント上限は 1MiB。AI 出力の生テキストを案件ごとに蓄積する設計では、案件数の増加に伴い**ある日突然、保存が全件失敗する**臨界点がある。また payload 全体の read-modify-write のため、複数端末（PC＋モバイル）同時操作時は後勝ち上書きで**片方の入力が黙って消える**
- useData.ts のマージ・エラー処理実装は未提供のため、上書き挙動の断定は**要確認**。ただし1MiB上限は実装に依らない構造リスク
- 問題がない点も付記：trash によるソフトデリート（types.ts L360-365）と localStorage フォールバックがあり、単発の誤操作への耐性は確保されている

### A2.【セキュリティ】認可がクライアント側に見える

- 根拠：App.tsx L44 で role を取得し、各 Tab に props として渡すのみ（L393-399）。viewer 制限の強制箇所はクライアント
- 根拠：CLAUDE.md「`GET /api/config` が Firebase 設定を JSON で返す」— つまり Firebase 設定は誰でも取得可能（これ自体は Firebase の通常設計）
- 問題：Firestore セキュリティルールが「認証済みユーザーのみ書込可」等になっていなければ、パスワード画面は見た目だけで、`workspace/main` に直接書き込み・読み取りが可能になる。営業先アカウント情報・会話ログという**第三者の個人情報を含むデータ**なので影響は大きい
- ルールファイル未提供のため断定しない：**要確認（優先度は最上位）**。確認方法：Firebase コンソールの Rules、または未認証クライアントからの読取テスト
- 付記：useAuth の probe 方式（404/405→自動admin）は CLAUDE.md 記載どおりならローカル開発専用挙動であり、本番で /api/auth が消えると認証が無効化される点も運用リスクとして要確認

### A3.【契約バグ】parseOS2 のフォールバックが「返信不可」ケースで誤キャプチャする

- 根拠：parser.ts L340 `replyA: raM ? cleanMsg(raM[1]) : cleanMsg(block(text, '次の返信案').split('\n')[0])`
- 根拠：OS2_行動判定_latest.md の【次の返信案】仕様 —「相手の最新反応が『いいねのみ』または『無反応』の場合：案A／案Bに文面を書かない。この枠を『次の接触条件』に切り替える」
- 問題：AI がルール通り「次の接触条件：相手の次の新規投稿が出たら…」と出力すると、案A の正規表現（L330）が不一致 → フォールバックがブロック先頭行を丸ごと replyA に格納する。つまり**送信してはいけない行動条件文が「返信案A」として保存・コピー可能になる**。S1-L はNT案件の標準ルート（OS2プロンプト自身の記述）なので、発生頻度は例外ではなく日常
- 同様に「なし（会話クローズ型のため即返信しない）」ケースも replyA='なし（…）' として保存される
- Tab2 側で表示制御している可能性はある（未提供・要確認）が、パーサ単体の契約としては誤り

### A4.【契約ギャップ】OS2 の [LOG]/[SLEEP]/[ARCHIVE]/[CLOSE] 行・DM_SCORE・再接触トリガーをパーサが捨てている

- 根拠：OS2プロンプトは判定ごとに [LOG] 行、休眠時 [SLEEP]（再接触トリガー付き）、保管時 [ARCHIVE]、クローズ時 [CLOSE] を必ず出力させる仕様
- 根拠：parser.ts L328-344 parseOS2 の戻り値にこれらのフィールドが一切ない
- 影響：休眠・保管の判定が出ても `recontact_date` / `state` への反映はユーザーの手入力頼み。App.tsx L109-152 の起動時通知は recontact_date が入っていて初めて機能するため、**転記漏れ＝通知漏れ＝案件の永久放置**に直結する（監査3-P2で改善案）
- Tab2 内の独自パース（os2Prompt.ts 等）で処理している可能性 → **要確認**。ただし parser.ts が「全OSで共通」とCLAUDE.mdに記載されている以上、共通パーサに無いのは契約上の穴

### A5.【型の不整合】S1-L ステップが型に存在しない

- 根拠：types.ts L3 `export type Step = 'S1' | 'S2' | 'S3' | 'S4' | 'S5'`
- 根拠：OS2プロンプト【現在ステップ】は「S1 / S1-L / S2 …」を出力し、parseOS2 は `step: firstLineOf(text, '現在ステップ')` を文字列で返す（parser.ts L333）
- 問題：'S1-L' を PipelineItem.currentStep に入れる経路があれば型違反（as キャストか黙殺）。Tab2 の代入箇所未提供のため**要確認**だが、プロンプト契約と型定義の乖離自体は確定事実

### A6.【技術的負債】データモデルの新旧二重構造

- 根拠：PipelineItem が旧系（history L273 / sentMessages L274 / replies L275 / analyses L272）と新系（touches L276 / ConversationTurn）を併存。Touch と ConversationTurn の双方に os2Judgment 等の同名フィールド（types.ts L26-29 と L70-73）
- 根拠：AppData に「旧フィールド（後方互換・移行元）」コメント付きの postStocks / ownPostAnalyses / myProfile（L586-589）、LogEntry は「旧機能・Tab4で集計に使用」（CLAUDE.md）
- 問題：どちらに書くのが正か新規コードから判別しづらく、集計（Tab4）が旧系依存のままだと新系のデータが数字に出ない恐れ。Tab4 実装未提供のため集計欠落は**要確認**
- 問題がない点：後方互換を意図したコメントが要所にあり、無秩序ではない

### A7.【軽微】App.tsx の小さな綻び（3件）

- L26-41 `PrefilledOS3` 型に `salesExpectationBreakdown` が無いが、L175 で prefill に詰めている。変数経由代入のため過剰プロパティ検査を素通りし、コンパイルは通るが型と実態が乖離
- L239 `new Date(p.lastContactDate || p.startDate || '')` — 両方欠損時 Invalid Date → NaN 比較 → 警告バッジから黙って漏れる（クラッシュはしない）
- L108-152 起動時チェックは `recontactCheckDone` ref で1回のみ。タブを閉じずに数日運用すると 48h チェック・再接触通知が発火しない（モバイルPWA的な使い方で顕在化）

### A8.【要確認】テスト構成

- テストファイル・テスト設定は未提供。CLAUDE.md の npm scripts にも test が無い。parser.ts のような正規表現契約はテストがないと退行しやすいため、存在しないなら parser のゴールデンテスト（実AI出力サンプル→期待オブジェクト）の新設を推奨（下記タスク分解T5）

## 修正タスク分解（承認後に Claude Code で委譲実行するための設計）

| ID | 作業内容 | 担当モデル | 完了条件 | 検証方法 | 参照ファイル |
|---|---|---|---|---|---|
| T1 | Firestoreセキュリティルールと useData.ts の書込・競合処理の棚卸し（調査のみ） | Haiku（棚卸し）→Opus（評価） | ルール全文と saveData の競合挙動の報告 | ルール原文の引用 | firestore.rules（要確認）, src/hooks/useData.ts |
| T2 | payload サイズ監視の追加（保存時に bytes 計測、800KB 超で警告トースト） | Sonnet | 警告が閾値超過時のみ表示 | 疑似データでの実行ログ | useData.ts, App.tsx |
| T3 | parseOS2 修正：「次の接触条件」「なし（会話クローズ型…）」を replyA と別フィールド（nextContactCondition / noReply）に分離 | Sonnet | 3パターンのAI出力サンプルで正しく分岐 | ユニットテスト差分 | parser.ts L328-344, OS2プロンプト |
| T4 | parseOS2 拡張：[LOG]/[SLEEP]/[ARCHIVE]/[CLOSE] 行・再接触トリガーの取込 → state/recontact_date 自動反映の提案設計 | Opus（設計）→Sonnet（実装） | 休眠判定貼付→recontact_date が自動セット | Tab2での動作ログ | parser.ts, Tab2.tsx（要確認）, types.ts |
| T5 | parser ゴールデンテスト新設（OS0/OS1/OS2/OS3 の実出力サンプル各3件） | Sonnet | `npm test` が通る | テスト実行ログ | parser.ts, 新規 tests/ |
| T6 | Step 型に 'S1-L' を追加 or 変換層を明示（設計判断） | Opus | 代入経路の全列挙と方針1つ | tsc エラーゼロ | types.ts L3, Tab2.tsx（要確認） |
| T7 | CLAUDE.md 矛盾3件の修正＋200行化（監査1の方針） | Sonnet | 200行以下・矛盾ゼロ | 行数と差分 | CLAUDE.md |
| T8 | 起動時チェックの定期再実行化（60分 interval or visibilitychange） | Sonnet | 放置タブでも通知発火 | 時刻改変テスト | App.tsx L108-152 |

実行順序：T1（安全確認）→ T3・T5（契約の穴＋回帰防止）→ T4 → T7 → T2・T8 → T6

**修正は適用していません。承認後に実行します。**

---

# 監査3：現在の運用に対する改善監査

## 前提

このOSの1案件あたりの標準ループは「スクショ/テキスト取得 → プロンプトコピー → 外部AIへ貼付 → 出力コピー → アプリへ貼付 → パース保存 → 送信文を手動転記して送信 → 反応を手動記録」。往復コストとパース失敗がそのまま接触件数（＝営業成果の分子）を削る構造なので、その2点を最優先で見た。

## 問題点（優先度順）

### P1.【誤判定の根】営業期待値スコアがパイプラインに一切流れていないのに、OS2の休眠/保管分岐がそれに依存している

- 根拠①：parser.ts L97-104 `extractSalesExp` は `salesExpectation: undefined, salesExpectationReason: undefined` を**常に**返す（ブロック生文のみ breakdown に保持）
- 根拠②：OS1_X プロンプトの現行出力フォーマット（L85-107）には営業期待値スコアの出力欄が存在しない（営業対象判定・優先セグメント・案件適合度に置換済み）
- 根拠③：にもかかわらず OS2 プロンプトは【営業期待値スコア】◯点を入力要求し、S1ループ運用ルールの出口分岐が「営業期待値35点以上/34点以下」で**休眠か次投稿維持かを決めている**
- 根拠④：types.ts L294 コメント「0-40, set at OS1, does not change」— 設計意図は残っているが供給源が消えている
- 影響：NT案件の標準ルートである S1-L の出口判定（休眠/保管/継続）が、毎回ユーザーの記憶か勘で埋める数値に依存。**有望案件の早期休眠（取りこぼし）と低確度案件への接触継続（労力漏れ）の両方が起きうる**
- 改善案（2択・承認後に選択）：
  - 案a：OS2 の分岐条件を現行データで賄える「案件適合度（高/中/低）＋優先セグメント」に書き換える（プロンプト改修のみ、コード不要）。旧互換の【営業期待値スコア】入力欄を削除
  - 案b：OS1 にスコア出力を復活させ parser で取り込む
  - 推奨は案a。理由：OS1側は既に適合度ベースへ移行済みで、逆行させるより整合が取れる
- 効果見積り：S1-L 出口判定のたびに発生する「スコアを思い出す/でっち上げる」判断コストがゼロになり、判定基準が全案件で統一される

### P2.【通知漏れ】休眠・保管の再接触日が自動で入らない

- 根拠：監査2-A4 と同一（parseOS2 が [SLEEP]/[ARCHIVE] を捨てる）＋ App.tsx L114-118 の通知は recontact_date 前提
- 影響：休眠判定→手入力を忘れた案件は sleeping のまま永久に通知されない。S∞ループの「休眠は正常な待機状態」という設計思想（OS2 重要原則①）が、実装上は「休眠＝実質クローズ」に化ける
- 改善案：判定が休眠/保管のとき、パース時に既定日（休眠+14日/保管+180日 — OS2記載の目安に準拠）を自動セットし、確認ダイアログで調整可能にする
- 効果見積り：手入力1件あたり2操作の削減より、**取りこぼしゼロ化**の価値が主。現在の休眠案件数ぶんの再接触機会が回収される

### P3.【毎回のトークン・時間コスト】プロンプトの死荷重

- 根拠：Human-Check Loop 約70行が OS0（全234行の約30%）と OS3（全123行の約57%）で機能条件を満たさないまま毎回送信される（監査1-P2a）
- 根拠：OS0 は末尾に除外済みアカウントリストを毎回付加する仕様（OS0 L233-234、CLAUDE.md「除外済みリストは OS⓪プロンプトに自動反映」）。excluded は運用継続で単調増加するため（types.ts L350-358 に削除期限なし）、**OS0 プロンプトは使うほど長く・遅く・高くなる**
- 改善案：①OS0/OS3 から Human-Check ブロックを削除（生成系OSのみ残す）②除外リストは「直近N件＋ハンドルのみ」に圧縮、またはアプリ側で判定一覧から既知ハンドルを事前除去してAIに送らない（クライアント側で照合可能：parseOS0 は handle をキーに持っている）
- 効果見積り：OS0 実行1回あたり入力トークン約30%減が即時に、除外リスト圧縮で長期の逓増を停止。外部AIの応答時間も比例して短縮

### P4.【手数】1案件あたりのコピペ往復が多く、S1量産のスループットを制限している

- 根拠：フロー定義（01_handoff_flow_and_scope.md）どおり、OS0→OS1→OS2 の各段で「プロンプトコピー→外部AI→出力貼付」が発生。OS1 は1アカウント=1実行（プロンプト341行＋スクショ3点セット）
- 根拠：types.ts L616 に S1_ACTION_BATCH キーが既に存在 — バッチ化の構想は既にある（実体ファイルは要確認）
- 改善案（運用＋UI）：
  - OS1 のバッチ版（1回で3〜5アカウント処理）を正式化。出力を区切りマーカーで分割パースする
  - Tab間遷移の setTimeout 待ち（App.tsx L183-186 の2.5秒、L219 の1.5秒）を即時遷移＋トースト並行に変更（1クローズあたり2.5秒×件数の固定待ちを除去）
- 効果見積り：OS1 バッチ化で1アカウントあたりの往復が約1/3〜1/5に。1日20アカウント処理なら往復操作が約80回→約25回

### P5.【パース失敗の静かな劣化】失敗が黙ってフィールド欠損になる

- 根拠：parser.ts の field/block は不一致時に空文字を返す（L4-14）。resolveSkipJudge（L29-38）は「SKIP理由の長さ>2」等のヒューリスティクスで、AIの言い回し次第で通過/SKIPが反転しうる。parseOS2 の誤キャプチャは監査2-A3のとおり
- 影響：欠損に気づくのが「詳細パネルを開いたとき」になり、開かなければ空データのままパイプラインを流れる
- 改善案（UI）：貼付→保存の間に「パース結果プレビュー」を挟み、必須フィールド（accountName / track / contactA / 判定）が空なら赤表示で保存前に知らせる。プロンプト側は末尾に固定終端マーカー（例：【END】）を追加し、途中切れ出力を検出可能にする
- 効果見積り：欠損の発見が「後日」→「貼付直後」に前倒し。再実行コスト（外部AIへの再往復）が案件単位で1回に抑えられる

### P6.【データ喪失リスク】単一ドキュメント上限と後勝ち上書き

- 根拠：監査2-A1 と同一
- 今すぐやること（コード変更前でも可）：ClosedDeal 化した案件の rawOutput / rawInput を Tab4 の Markdown エクスポートで退避してから手動削除する運用を暫定ルール化
- 恒久対応：payload サイズ監視（T2）＋ raw系の別ドキュメント分離

### P7.【判断の遅さ・後回しで可】起動時のみのチェック、通知ベルの飛び先が固定

- 根拠：App.tsx L108（起動時1回）、L320 通知ベルは常に tab2 へ遷移するだけで対象アイテムを開かない（再接触トーストのみ L149 で focus 設定あり）
- 改善案：ベルタップで「要対応リスト」モーダル（48h超・再接触日到来・7日/30日警告を1画面に集約）。優先度は P1〜P5 より低い

## 分類別の改善案まとめ

**プロンプト改善（コード不要・最速で効く）**
1. OS2 の休眠/保管分岐を「案件適合度＋優先セグメント」基準へ書換え、【営業期待値スコア】入力欄を廃止（P1案a）
2. OS0・OS3 から Human-Check ブロック削除（P3）
3. 全生成系プロンプトに終端マーカー追加（P5）

**データ設計改善**
4. [SLEEP]/[ARCHIVE]/[CLOSE] のパース取込＋recontact_date 自動セット（P2）
5. payload サイズ監視と raw系の分離（P6）
6. salesExpectation 系フィールドの廃止 or 供給復活の二択決着（P1、types.ts L294-297 の整理）

**UI改善**
7. パース結果プレビュー＋必須欠損の赤表示（P5）
8. クローズ/戻し時の固定 setTimeout 廃止（P4）
9. 要対応リストモーダル（P7）

**運用改善**
10. OS1 バッチ運用の正式化（P4、S1_ACTION_BATCH の実態確認後）
11. 除外リストのアプリ側事前照合（AIに送らない）（P3）
12. クローズ済み案件 raw データの定期退避（P6 暫定）

## 今すぐ直すべきもの / 後回しでよいもの

- 今すぐ：P1（判定基準の空洞化）、P2（通知漏れ）、P3のOS0除外リスト圧縮（逓増コストの停止）
- 次に：P5（プレビュー）、P4（バッチ化）、P6恒久対応
- 後回し可：P7、ハンドオフ文書統合、旧データモデル整理

---

# 統合所見

**最初に着手すべきは P1（営業期待値スコアの空洞化）で、手段は「OS2 プロンプトの分岐基準を案件適合度＋優先セグメントへ書き換える」こと。**

理由は3つ。第一に、これは営業成果の分子（どの案件に接触し続け、どれを寝かせるか）を直接決める判定ロジックが、供給の絶えたデータに依存している唯一の箇所であり、誤判定が今この瞬間も静かに蓄積している。第二に、修正はプロンプトファイル1本の編集＋`npm run deploy` で完結し、コード変更もマイグレーションも不要（このプロジェクトの「プロンプト＝契約」という設計思想に最も沿った直し方）。第三に、この決着（適合度基準に一本化）が types.ts の salesExpectation 系フィールド整理・parser の extractSalesExp 除去という後続タスクの前提になるため、順序として先頭に置くと後続の手戻りがない。

その直後に P2（[SLEEP]/[ARCHIVE] の取込）を入れると、S∞ループが設計どおり「休眠＝待機、保管＝長期待機」として初めて機械的に回り始める。ここまでで「判定の正しさ」と「再接触の確実さ」が揃い、以降のバッチ化・プレビューは純粋なスループット改善として積める。

指示ファイル（CLAUDE.md）の矛盾3件は、Claude Code に修正を委譲する前提なら P1 と同時に直すべき。実装を触る前に地図を直さないと、委譲先が古い地図で作業する。

**修正・設定変更・ファイル削除・統合は一切適用していません。すべて承認後に実行します。**

## 要確認事項（承認判断の前に確認を推奨）

1. Firestore セキュリティルールの実態（最重要・監査2-A2）
2. AGENTS.md / .claude/ の存在と内容（監査1の対象漏れ）
3. S1_ACTION_BATCH の実体ファイル有無（P4の前提）
4. Tab2 内の OS2 出力処理（os2Prompt.ts）が [SLEEP]/[ARCHIVE] を独自に拾っていないか（P2の重複回避）
5. Tab4 の集計が新系（touches）を拾っているか旧系（logs/history）依存か（監査2-A6）
